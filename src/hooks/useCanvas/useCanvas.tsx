import { useRef, useEffect, useState } from 'react';
import VideoTrack from '../../components/VideoTrack/VideoTrack';
import Canvas from '../../components/CanvasTrack/CanvasTrack';

// creates and draws
// get the video track ref
//canvas should be getting the videoref and attaching a canvas element to it
export default function useCanvas() {}
